# Ralph Progress Log
Started: 2026-07-28T20:49:07+08:00
---

## Codebase Patterns
- A new CHI translation unit must be added both as a production `Source` and
  to every standalone `GTest` source list that directly links its callers.
- The four HNF baseline GTests build under `build/RISCV/mem/cache/CHI/`; their
  expected suite counts are 15 POCQ, 2 SEQ POCQ, 10 SLCSF, and 9 controller.
- Delayed CHI service messages should copy scalar flit metadata and own all
  variable payloads with value containers such as `vector` and `optional`.
- Couple a delayed operation's kind to its payload with a restricted variant;
  derive the kind from the active alternative instead of storing two fields
  that can disagree.
- Derive terminal response identity from the typed request and keep generic
  status/payload construction private so invalid combinations cannot compile.
- Treat double-buffer queue sizes as one shared logical capacity across all
  registered stages rather than giving each physical deque the full capacity.
- For retryable owned messages, accept an rvalue reference but move from it
  only after admission succeeds so rejection preserves caller ownership.
- Represent an in-flight request as its terminal response reservation, then
  count in-flight, pending, and visible states as one bounded occupancy.
- Express configurable service latency as an absolute completion cycle
  captured at issue, and run completion before issue in each wakeup so newly
  issued work cannot finish in the same cycle.
- Translate owned asynchronous requests into synchronous backend calls only at
  their modeled completion boundary, and map backend retry decisions to typed
  terminal Replay responses rather than Done payload flags.
- Derive replay deadlines from the absolute gem5 `Tick` at the completion
  boundary; service-local cycle numbers are not valid retry timestamps.
- Keep a storage/reset epoch independent from read-only lookup-access
  accounting so replacement touches cannot invalidate coherence snapshots.
- When an embedded timed service depends on its owner for clock edges, share
  one advance/order/work-decision helper between production scheduling and
  smoke tests so tests exercise the real reschedule boundary.
- Model an asynchronous controller operation with explicit issue-pending,
  waiting, and response-latched phases; retry only the owned pending request
  and continue the graph from a later service boundary.
- At a registered response boundary, validate the complete operation and
  identity pair, copy owned payload state under a bounded consume budget, and
  defer graph continuation until the next controller service cycle.
- Validate a miss token against the generation of its recorded candidate way,
  not the way a later LRU probe would select; replacement touches may reorder
  candidates without mutating the recorded storage snapshot.
- Model a staged mutation as one in-flight owner of both its request and
  terminal response reservation; acquire transient resources before the sole
  write stage and convert the same reservation to a pending response only at
  the final registered boundary.
- Carry the lookup CommitToken by value in every delayed mutation request and
  validate it in U0 before resource acquisition; stale snapshots become owned
  terminal Replay responses with absolute retry deadlines.
- Preflight unsupported dirty SLC displacement before acquiring transient
  resources, while recognizing semantic no-op writes that need no allocation.
- Revalidate a versioned mutation snapshot immediately before the sole write
  after any resource stall, and use its recorded ways for both preflight and
  allocation so replacement touches cannot redirect the commit.
- Assign each issued service request an earliest completion cycle from its
  operation class; deadline-driven catch-up may traverse multiple structural
  stages on one edge but must stop at a resource stall, while early Replay
  explicitly replaces the deadline with the one-cycle completion rule.
- Keep the sole persistent write stage parked until its configured service
  deadline; pre-deadline decode and resource preparation may advance, but
  terminal catch-up must not make architectural mutation visible early.
- Latch a delayed Replay reason when detected, then construct its absolute
  retry deadline at terminal cleanup so pipeline delay does not consume the
  configured replay penalty.
- Test staged stale-token recovery at both resource boundaries: one case must
  invalidate while stalled before acquisition, and another after acquisition
  but before the sole write, so reservation ownership and cleanup stay proven.
- Pass the owner's absolute Tick into controller service boundaries that gate
  retries; a no-argument test seam must not be used by production scheduling.
- When multiple transaction classes share asynchronous issue and wait nodes,
  discriminate their terminal graph edges and response payloads by operation
  kind so each class emits only its own protocol completion.
- Route side effects produced by periodic controller service through an owned
  deferred queue, include that queue in work detection, and let the owning
  event loop drain it exactly once on a later scheduling boundary.
- For a directory-only transaction that still requires token validation,
  perform a registered lookup first and issue the typed mutation from the
  lookup-completion graph edge; emit protocol completion only after Done.
---

## 2026-07-28 21:01 - US-001
- Story: Extract the synchronous SLC/SF storage backend
- What was implemented: Moved all SLC/SF lines, replacement metadata, SEQ
  storage, reservations, and synchronous semantic operations into the plain
  `HnfSLCSFBackend` C++ class. Kept `HnfSLCSF` as a header-only synchronous
  compatibility adapter inheriting the backend, with unchanged public calls.
- Files changed: `src/mem/cache/CHI/HnfSLCSFBackend.hh`,
  `src/mem/cache/CHI/HnfSLCSFBackend.cc`, `src/mem/cache/CHI/HnfSLCSF.hh`,
  `src/mem/cache/CHI/HnfSLCSF.cc`, `src/mem/cache/CHI/SConscript`, `prd.json`,
  and `progress.txt`.
- Checks run and their results: Built all four RISCV CHI GTest binaries with
  SCons successfully; ran all 36 tests successfully (15 POCQ, 2 SEQ POCQ,
  10 SLCSF, 9 controller); normalized old/new implementation diff was empty;
  `git diff --check` passed.
- Learnings for future iterations: The compatibility type can remain stable
  while storage evolves behind it, and standalone GTests require explicit
  linkage of newly extracted implementation units.
---

## 2026-07-28 21:07 - US-002
- Story: Define owned typed SLCSF request messages
- What was implemented: Added a non-copyable monotonic `SlcSfReqId` allocator,
  a common copied request header with POCQ, address, CHI, QoS, and trace fields,
  and owned Lookup, Fill, Update, and Evict request variants. Added mutation
  kinds for every synchronous storage operation plus SF-evict completion and
  dirty-victim release, with owned line, mask, victim, and snoop payloads.
- Files changed: `src/mem/cache/CHI/HnfSLCSFRequest.hh`,
  `src/mem/cache/CHI/HnfSLCSF.test.cc`, `prd.json`, and `progress.txt`.
- Checks run and their results: Built all four RISCV CHI GTest binaries with
  SCons successfully; all 39 tests passed (15 POCQ, 2 SEQ POCQ, 13 SLCSF,
  including 3 new request tests, and 9 controller); `git diff --check` passed;
  the changed C++ files passed `python3 util/style.py`.
- Learnings for future iterations: A strong request-ID type prevents accidental
  reuse of LinkLayer sequence values, and queue-safe payloads can be introduced
  without changing the temporary synchronous adapter.
---

## 2026-07-28 21:11 - US-002 acceptance audit correction
- Independent acceptance review found that the committed request model cannot
  yet represent `completeSfEvict(id, data, dirty)` unambiguously, conflates SLC
  and SF victim state, permits invalid kind/payload combinations, and does not
  test parameter-preserving mappings for every synchronous operation including
  `fillCleanShared`.
- US-002 was reset to `passes: false`; its `notes` field contains the concrete
  repair requirements. Commit `e7b1e6bb0d` remains as the base to repair.
- Ralph recovery files for US-002: the current uncommitted changes to
  `prd.json` and `progress.txt` are intentional story-scoped recovery metadata.
  Review them, retain them, and stage them with the repaired US-002 files.
---

## 2026-07-28 21:18 - US-002
- Story: Define owned typed SLCSF request messages
- What was implemented: Repaired the typed request model by replacing freely
  combinable kind and optional payload fields with restricted Fill, Update,
  and Evict operation variants. Added `fillCleanShared`, strong SEQ and dirty
  victim identities, distinct owned SLC/SF victim snapshots, unambiguous
  CompleteSfEvict and ReleaseDirtyVictim update payloads, and factories that
  preserve every synchronous operation parameter.
- Files changed: `src/mem/cache/CHI/HnfSLCSFRequest.hh`,
  `src/mem/cache/CHI/HnfSLCSF.test.cc`, `prd.json`, and `progress.txt`.
- Checks run and their results: Built all four RISCV CHI GTest binaries with
  SCons successfully; all 39 tests passed (15 POCQ, 2 SEQ POCQ, 13 SLCSF,
  and 9 controller), retaining all 36 baseline passes; changed C++ files
  passed `python3 util/style.py`; `git diff --check` passed.
- Learnings for future iterations: Operation-specific variants prevent kind
  and payload drift at compile time, while value-taking factories provide one
  auditable ownership boundary for delayed requests.
---

## 2026-07-28 21:30 - US-003 interrupted acceptance audit
- The first response/token draft was stopped before tracking updates or commit.
  It models the three terminal statuses and full CommitToken fields, but only
  tests four broad operation categories and exposes construction paths that can
  pair an arbitrary kind, status, and payload.
- Repair requirements are recorded in US-003's `notes`: derive concrete
  operation identity from typed requests, make mismatches unconstructible, and
  test Lookup plus all ten mutations, both Replay redo choices, absolute retry
  ticks, token sentinels, and owned response payload lifetimes.
- Ralph recovery files for US-003: the current uncommitted
  `src/mem/cache/CHI/HnfSLCSFResponse.hh`,
  `src/mem/cache/CHI/HnfSLCSF.test.cc`, `prd.json`, and `progress.txt` are
  intentional story-scoped work. Review, retain, repair, and stage only these
  files when US-003 passes.
---

## 2026-07-28 21:35 - US-003
- Story: Define terminal responses, replay payloads, and CommitToken
- What was implemented: Added self-contained terminal responses with concrete
  identities for Lookup and all ten mutation operations, typed-request-derived
  Done, Replay, and Error factories, absolute replay timing with redo control,
  and full versioned SLC/SF CommitToken snapshots. Added mapping, construction,
  sentinel-field, and producing-stack lifetime coverage for owned payloads.
- Files changed: `src/mem/cache/CHI/HnfSLCSFResponse.hh`,
  `src/mem/cache/CHI/HnfSLCSF.test.cc`, `prd.json`, and `progress.txt`.
- Checks run and their results: Built all four RISCV CHI GTest binaries with
  SCons successfully; all 43 tests passed (15 POCQ, 2 SEQ POCQ, 17 SLCSF,
  and 9 controller), retaining all 36 baseline passes; changed C++ files
  passed `python3 util/style.py`; `git diff --check` passed.
- Learnings for future iterations: Concrete operation identity should be
  derived at the typed request boundary, while response payload ownership can
  be validated by returning messages built from local producing-stack data.
---

## 2026-07-28 21:42 - US-004
- Story: Implement bounded request double buffering and registered credit
- What was implemented: Added owned request admission with Accepted, NoCredit,
  Initializing, and Draining outcomes; registered ingress-to-ready promotion;
  bounded ready-to-in-flight issue; shared outstanding accounting; per-wakeup
  credit latching and same-wakeup decrement; lifecycle admission gates; and
  fail-fast queue, in-flight, and issue-width validation.
- Files changed: `src/mem/cache/CHI/HnfSLCSF.hh`,
  `src/mem/cache/CHI/HnfSLCSF.cc`,
  `src/mem/cache/CHI/HnfSLCSF.test.cc`, `prd.json`, and `progress.txt`.
- Checks run and their results: Built all four RISCV CHI GTest binaries with
  SCons successfully; all 48 tests passed (15 POCQ, 2 SEQ POCQ, 22 SLCSF,
  including 5 new request-queue tests, and 9 controller), retaining all 36
  baseline passes; changed C++ files passed `python3 util/style.py`;
  `git diff --check` passed.
- Learnings for future iterations: Registered credit must account for ingress,
  ready, and non-terminal in-flight requests as one logical capacity, and an
  rvalue request remains retryable when rejection occurs before the move.
---

## 2026-07-28 21:48 - US-005
- Story: Implement response double buffering and terminal reservations
- What was implemented: Added shared-capacity terminal response reservations,
  registered pending-to-visible response promotion, atomic reservation-to-
  pending completion, visible-response consumption, response-aware work
  detection, and issue stalling under response backpressure. Each accepted
  request now produces one typed terminal response while rejected requests
  produce none.
- Files changed: `src/mem/cache/CHI/HnfSLCSF.hh`,
  `src/mem/cache/CHI/HnfSLCSF.cc`,
  `src/mem/cache/CHI/HnfSLCSF.test.cc`, `prd.json`, and `progress.txt`.
- Checks run and their results: Built all four RISCV CHI GTest binaries with
  SCons successfully; all 52 tests passed (15 POCQ, 2 SEQ POCQ, 26 SLCSF,
  including 4 new response-queue tests, and 9 controller), retaining all 36
  baseline passes; changed C++ files passed `python3 util/style.py`;
  `git diff --check` passed.
- Learnings for future iterations: Reserving response capacity before issue
  makes completion non-blocking, while pending and visible response buffers
  can remain registered stages without multiplying the configured capacity.
---

## 2026-07-28 21:49 - US-005 interrupted acceptance audit
- The first response-buffer draft built successfully and passed 52 tests, but
  independent acceptance review stopped it before commit because two required
  properties were not yet demonstrated safely.
- Repair the issue boundary so terminal capacity is reserved before a request
  is moved into in-flight state; keep reservation and issue coupled so no path
  can expose an issued request without its durable terminal slot.
- Add mixed-pipe, out-of-acceptance-order coverage and verify every completion
  by `(pocEntryId, reqId)`, rather than assuming the visible queue follows
  request FIFO order. Concrete requirements are also recorded in US-005 notes.
- Ralph recovery files for US-005 are the currently staged changes to
  `src/mem/cache/CHI/HnfSLCSF.hh`, `src/mem/cache/CHI/HnfSLCSF.cc`,
  `src/mem/cache/CHI/HnfSLCSF.test.cc`, `prd.json`, and `progress.txt`.
  Review and repair this intentional story-scoped draft, then stage only these
  files when US-005 genuinely passes.
---

## 2026-07-28 21:55 - US-005
- Story: Implement response double buffering and terminal reservations
- What was implemented: Completed the response-buffer recovery by making
  in-flight queue membership itself the durable terminal reservation, so a
  request is never issued before capacity is held. Added mixed lookup/fill
  coverage proving responses can complete as 61, 63, then 62 and are matched
  by `(pocEntryId, reqId)` rather than acceptance order.
- Files changed: `src/mem/cache/CHI/HnfSLCSF.hh`,
  `src/mem/cache/CHI/HnfSLCSF.cc`,
  `src/mem/cache/CHI/HnfSLCSF.test.cc`, `prd.json`, and `progress.txt`.
- Checks run and their results: Built all four RISCV CHI GTest binaries with
  SCons successfully; all 53 tests passed (15 POCQ, 2 SEQ POCQ, 27 SLCSF,
  and 9 controller), retaining all 36 baseline passes; changed C++ files
  passed `python3 util/style.py`; `git diff --check` passed.
- Learnings for future iterations: Deriving reservation count from in-flight
  ownership removes an independently mutable accounting state, and mixed-pipe
  tests should compare response identity pairs without relying on queue order.
---

## 2026-07-28 21:57 - US-005 final identity-sentinel audit
- The reservation-order and mixed-pipe completion gaps were repaired and all
  53 tests passed, but review stopped the commit because the mixed-pipe test
  used equal values for `pocEntryId` and `reqId` and therefore could not prove
  that both identity fields survive independently.
- The recovery test now uses request IDs 61/63/62 with distinct POC entry IDs
  601/603/602. Re-run the full checks and retain the in-flight-owned terminal
  reservation design before marking US-005 complete.
- The intentional recovery set remains `src/mem/cache/CHI/HnfSLCSF.hh`,
  `src/mem/cache/CHI/HnfSLCSF.cc`, `src/mem/cache/CHI/HnfSLCSF.test.cc`,
  `prd.json`, and `progress.txt`; unrelated user files remain out of scope.
---

## 2026-07-28 21:59 - US-005
- Story: Implement response double buffering and terminal reservations
- What was implemented: Finalized shared-capacity response buffering with
  in-flight-owned terminal reservations, registered pending-to-visible
  promotion, atomic terminal completion, backpressure-safe issue, and
  out-of-order mixed-pipe completion matched by independent POC entry and
  request identities.
- Files changed: `src/mem/cache/CHI/HnfSLCSF.hh`,
  `src/mem/cache/CHI/HnfSLCSF.cc`,
  `src/mem/cache/CHI/HnfSLCSF.test.cc`, `prd.json`, and `progress.txt`.
- Checks run and their results: Built all four RISCV CHI GTest binaries with
  SCons successfully; all 53 tests passed (15 POCQ, 2 SEQ POCQ, 27 SLCSF,
  and 9 controller); changed C++ files passed `python3 util/style.py`;
  `git diff --check` and `git diff --cached --check` passed.
- Learnings for future iterations: Use independent sentinel values for every
  component of a compound identity so tests prove preservation rather than
  accidentally passing through aliased values.
---

## 2026-07-28 22:05 - US-006
- Story: Implement the non-zero-latency lookup pipeline
- What was implemented: Added runtime-configurable lookup service latency with
  a four-cycle default, construction validation, defensive one-cycle minimum,
  per-request issue and completion cycles, and a separately registered
  response-visible boundary. Preserved mixed-pipe out-of-order completion and
  terminal response reservations while lookup requests remain in flight.
- Files changed: `src/mem/cache/CHI/HnfSLCSF.hh`,
  `src/mem/cache/CHI/HnfSLCSF.cc`,
  `src/mem/cache/CHI/HnfSLCSF.test.cc`, `prd.json`, and `progress.txt`.
- Checks run and their results: Built all four RISCV CHI GTest binaries with
  SCons successfully; all 57 tests passed (15 POCQ, 2 SEQ POCQ, 31 SLCSF,
  and 9 controller), retaining all 36 baseline passes; changed C++ files
  passed `python3 util/style.py`; `git diff --check` passed.
- Learnings for future iterations: Absolute ready cycles let runtime latency
  vary without a compile-time pipeline depth, while wakeup ordering enforces
  acceptance, issue, completion, and visibility as distinct boundaries.
---

## 2026-07-28 22:06 - US-006 acceptance-audit recovery
- The first timing draft built and passed its queue/timestamp tests, but an
  independent acceptance audit found that asynchronous Lookup completion still
  returned an empty `HnfSlcLookupResult` and never called the synchronous
  backend. It therefore did not make any SLC, SF, policy, or SEQ decision
  timing-visible and must remain `passes: false`.
- Preserve the absolute issue/completion-cycle design and US-005 response
  accounting. At modeled Lookup completion, translate the owned request into a
  backend lookup (or equivalent staged probes), return its real payload, and
  add payload-sensitive seeded SLC/SF and SEQ replay tests across the registered
  pending-to-visible boundary.
- Map a backend `result.replay` decision to terminal status `Replay` with a
  meaningful reason, `redoLookup=true`, and an absolute retry tick; `Done`
  carrying a lookup result whose replay bit is set would violate the terminal
  response contract established by US-003.
- The intentional recovery set is `src/mem/cache/CHI/HnfSLCSF.hh`,
  `src/mem/cache/CHI/HnfSLCSF.cc`, `src/mem/cache/CHI/HnfSLCSF.test.cc`,
  `prd.json`, and `progress.txt`; unrelated user files remain out of scope.
---

## 2026-07-28 22:11 - US-006
- Story: Implement the non-zero-latency lookup pipeline
- What was implemented: Completed the interrupted timing draft by translating
  owned Lookup requests into backend probes only at their configured completion
  cycle. Done responses now carry the real SLC/SF result, while backend SEQ
  conflicts produce typed Replay responses with redoLookup and an absolute
  retry tick through the registered pending-to-visible boundary.
- Files changed: `src/mem/cache/CHI/HnfSLCSF.hh`,
  `src/mem/cache/CHI/HnfSLCSF.cc`,
  `src/mem/cache/CHI/HnfSLCSF.test.cc`, `prd.json`, and `progress.txt`.
- Checks run and their results: Built all four RISCV CHI GTest binaries with
  SCons successfully; all 59 tests passed (15 POCQ, 2 SEQ POCQ, 33 SLCSF,
  and 9 controller), retaining all 36 baseline passes; changed C++ files
  passed `python3 util/style.py`; `git diff --check` and
  `git diff --cached --check` passed.
- Learnings for future iterations: Keep semantic execution at the modeled
  completion boundary, and represent backend replay decisions as terminal
  control outcomes instead of successful data payloads.
---

## 2026-07-28 22:12 - US-006 blocked finalization
- Implementation, build, style, diff, and all 59 tests passed, but the required
  exact commit subject `mem-cache: US-006 - Implement the non-zero-latency
  lookup pipeline` is 66 characters. The repository commit hook rejects headers
  longer than 65 characters.
- Attempted the exact required `git commit -m` command without bypassing hooks;
  the hook cancelled the commit with `66 > 65`. Shortening the PRD story title
  would violate the Ralph instruction requiring the exact subject, while
  `--no-verify` is explicitly forbidden.
- US-006 remains `passes: false`. Its documented five-file recovery set remains
  staged, and unrelated user changes remain unstaged.
---

## 2026-07-28 22:14 - US-006 absolute-tick audit recovery
- Independent review found one remaining P1: `wakeupCycle + 1` is a local child
  cycle number, not an absolute gem5 `Tick`. At a nonzero simulator time or
  with a multi-tick clock period, the replay deadline would already be in the
  past and permit an immediate retry.
- Preserve the verified real backend payload and terminal `SeqConflict` Replay
  path. Let wakeup receive or record the absolute completion Tick (or perform
  an equivalent explicit conversion), require the retry deadline to be later
  than that Tick, and test it using nonzero absolute wakeup values.
- The repository hook's 65-character limit takes precedence over spelling out
  an overlong PRD title. Keep `mem-cache: US-006 -` and use an unambiguous
  hook-compliant concise title; never bypass the hook.
- The intentional recovery set remains `src/mem/cache/CHI/HnfSLCSF.hh`,
  `src/mem/cache/CHI/HnfSLCSF.cc`, `src/mem/cache/CHI/HnfSLCSF.test.cc`,
  `prd.json`, and `progress.txt`; unrelated user files remain out of scope.
---

## 2026-07-28 22:21 - US-006
- Story: Implement the non-zero-latency lookup pipeline
- What was implemented: Finalized the registered lookup pipeline with a
  runtime-validated four-cycle default, issue and completion cycle tracking,
  real backend lookup execution at completion, and pending-to-visible response
  registration. Backend SEQ conflicts now return terminal Replay responses
  whose retry deadline is derived from the absolute completion Tick.
- Files changed: `src/mem/cache/CHI/HnfSLCSF.hh`,
  `src/mem/cache/CHI/HnfSLCSF.cc`,
  `src/mem/cache/CHI/HnfSLCSF.test.cc`, `prd.json`, and `progress.txt`.
- Checks run and their results: Built all four RISCV CHI GTest binaries with
  SCons successfully; all 59 tests passed (15 POCQ, 2 SEQ POCQ, 33 SLCSF,
  and 9 controller), retaining all 36 baseline passes; changed C++ files
  passed `python3 util/style.py`; `git diff --check` and
  `git diff --cached --check` passed.
- Learnings for future iterations: Keep service latency in child-cycle units,
  but construct externally visible retry times from an absolute gem5 Tick at
  the semantic completion boundary.
---

## 2026-07-28 22:27 - US-007 acceptance-audit recovery
- The first semantic-preservation draft correctly separated a pure const probe
  from one explicit replacement-access commit and populated real CommitToken
  array snapshots, but an independent audit found that `recordAccess()` also
  incremented the global `lookupEpoch` on every successful lookup.
- That design would make an older token stale after any unrelated read-only
  lookup, contradicting the planned `LookupTouchDoesNotInvalidateToken`
  invariant. Keep `lookupEpoch` stable across probes and replacement touches,
  and use a separate access counter to verify non-Replay +1 and Replay +0.
- Strengthen the required `SeqHitReplayHasNoSideEffects` regression to compare
  replacement stamps/access count, generations, lookup facts, SEQ occupancy,
  and the complete pending victim identity. A generation-only check cannot
  detect an erroneous replacement-policy touch.
- Repair the draft test's reference to translation-unit-private `SnpOp`, and
  validate semantic fields—not only hit bits—in all four SLC/SF combinations.
- Preserve the current five-file draft and all US-006 timing/reservation tests;
  this interrupted attempt made no commit and consumes no effective Ralph
  iteration.
---

## 2026-07-28 22:34 - US-007
- Story: Preserve lookup semantics and deterministic replacement order
- What was implemented: Split the pure lookup probe from its single explicit
  replacement-access commit, returned the full synchronous lookup facts and
  pre-touch CommitToken snapshots through the delayed pipeline, and separated
  stable token epoch from lookup-access accounting. Added observable
  replacement stamps and regressions for all four SLC/SF hit combinations,
  Replay side effects, and deterministic victim selection.
- Files changed: `src/mem/cache/CHI/HnfSLCSFBackend.hh`,
  `src/mem/cache/CHI/HnfSLCSFBackend.cc`,
  `src/mem/cache/CHI/HnfSLCSF.cc`,
  `src/mem/cache/CHI/HnfSLCSF.test.cc`, `prd.json`, and `progress.txt`.
- Checks run and their results: Built all four RISCV CHI GTest binaries with
  SCons successfully; all 63 tests passed (15 POCQ, 2 SEQ POCQ, 37 SLCSF,
  and 9 controller), retaining all 36 baseline passes; changed C++ files
  passed `python3 util/style.py`; `git diff --check` passed.
- Learnings for future iterations: A replacement touch needs its own monotonic
  observation counter; token epochs should represent storage lifecycle, while
  pre-touch set, way, generation, and replacement stamps preserve the exact
  decision that produced the lookup response.
---

## 2026-07-28 22:39 - US-008 acceptance-audit recovery
- The first stage-A draft exposed and mapped the complete parameter set and
  called `slcsf.wakeup(curTick())` once before the link layer, but it was
  interrupted before build, pass marking, or commit after two independent
  acceptance gaps were found.
- Restore the source-plan MVP default `enable_set_lock=False`; the draft used
  true in Python, C++, and its regression. Reject an unsafe `max_inflight > 1`
  configuration when that transition control is disabled.
- Replace the direct `while (model.hasWork()) model.wakeup()` smoke test with a
  fixture or production scheduling seam that exercises the same combined-work
  decision as `HomeNodeFull::wakeup()`. It must reach a visible response with
  no additional RX flit, consume it, and then prove scheduling stops at idle.
- Preserve the intentional five-file draft in `HomeNodeFull.py`,
  `HomeNodeFull.cc`, `HnfSLCSF.hh`, `HnfSLCSF.cc`, and
  `HnfSLCSF.test.cc`; this interrupted attempt made no commit and consumes no
  effective Ralph iteration.
---

## 2026-07-28 22:52 - US-008
- Story: Add stage-A parameters and HomeNode scheduling
- What was implemented: Added the complete `slcsf_`-prefixed stage-A parameter
  surface and exact HomeNode-to-pipeline mapping, safe MVP defaults and
  validation, and an embedded service driver that advances SLCSF once before
  LinkLayer work and returns the combined owner reschedule decision. Added
  default/override mapping coverage and a production-seam smoke test that
  completes and consumes a lookup without another RX flit, then stops idle
  scheduling.
- Files changed: `src/mem/cache/CHI/HomeNodeFull.py`,
  `src/mem/cache/CHI/HomeNodeFull.cc`, `src/mem/cache/CHI/HnfSLCSF.hh`,
  `src/mem/cache/CHI/HnfSLCSF.cc`, `src/mem/cache/CHI/HnfSLCSF.test.cc`,
  `prd.json`, and `progress.txt`.
- Checks run and their results: Built `build/RISCV/gem5.opt` successfully;
  instantiated default and non-default `HomeNodeFull` configurations; built
  all four RISCV CHI GTests; all 67 tests passed (15 POCQ, 2 SEQ POCQ,
  41 SLCSF, and 9 controller); changed sources passed `python3 util/style.py`;
  `git diff --check` passed.
- Learnings for future iterations: A shared owner-driving seam can prove both
  registered child-before-link ordering and idle-stop scheduling without a
  heavyweight HomeNode fixture, while a named parameter mapper keeps Python
  and C++ configuration surfaces auditable.
---

## 2026-07-28 22:55 - US-009 preflight
- This is the tenth effective Ralph iteration. Interrupted, uncommitted audit
  attempts were not counted; do not start another story after this iteration.
- Allocate one CC-owned SLCSF request ID per lookup and retain the exact owned
  request across `NoCredit`/`Initializing` retries. Track explicit
  `IssuePending`, `Waiting`, and `ResponseLatched` entry phases so periodic
  service cannot enqueue a duplicate while an operation is outstanding.
- `DoSlcLookup` may only construct and try to enqueue; it must not perform a
  synchronous backend lookup, recursively deliver `SlcLookupDone`, or advance
  graph completion in the request cycle.
- Adapt controller tests to a production-order pump (`slcsf.wakeup(tick)` then
  `cc.serviceInternalWork()`), including queue-backpressure eventual progress
  with stable reqId and repeated-service no-duplicate coverage. Keep US-010's
  strict matching and parameterized response-consume semantics out of scope,
  except for the minimum separated latch/continuation needed for liveness.
---

## 2026-07-28 23:04 - US-009
- Story: Issue POCQ lookup requests asynchronously
- What was implemented: Replaced synchronous POCQ lookup completion with a
  CC-owned monotonic request allocator and per-entry IssuePending, Waiting,
  and ResponseLatched phases. Lookup requests now own copied metadata, retain
  their exact object and ID across admission backpressure, latch terminal
  responses before later graph continuation, and cannot be issued twice.
- Files changed: `src/mem/cache/CHI/HnfCoherencyController.hh`,
  `src/mem/cache/CHI/HnfCoherencyController.cc`,
  `src/mem/cache/CHI/HnfCoherencyController.test.cc`, `prd.json`, and
  `progress.txt`.
- Checks run and their results: Built `build/RISCV/gem5.opt` and all four CHI
  GTest binaries successfully; all 69 tests passed (15 POCQ, 2 SEQ POCQ,
  41 SLCSF, and 11 controller); changed C++ files passed
  `python3 util/style.py`; `git diff --check` passed.
- Learnings for future iterations: Keeping rejected requests in an explicit
  issue-pending phase preserves ownership and identity, while latching visible
  responses at the end of controller service prevents recursive same-cycle
  graph continuation.
---

## 2026-07-29 07:26 - US-010
- Story: Consume lookup responses on a later CC cycle
- What was implemented: Added budgeted visible-response consumption with
  strict operation and `(pocEntryId, reqId)` validation. Lookup data, dirty
  state, semantic facts, and CommitToken are copied into the CC entry at the
  response-latch boundary, while `SlcLookupDone` graph continuation remains
  registered until the following controller service cycle. Unexpected,
  duplicate, and stale responses now share one fail-fast validation path.
- Files changed: `src/mem/cache/CHI/HnfCoherencyController.hh`,
  `src/mem/cache/CHI/HnfCoherencyController.cc`,
  `src/mem/cache/CHI/HnfCoherencyController.test.cc`, `prd.json`, and
  `progress.txt`.
- Checks run and their results: Built `build/RISCV/gem5.opt` and all four CHI
  GTest binaries successfully; all 72 tests passed (15 POCQ, 2 SEQ POCQ,
  41 SLCSF, and 14 controller), retaining all 36 baseline passes; the default
  four-core litmus passed all 7 workloads with
  `CHI_LITMUS_PASS total_errors=0`; changed C++ files passed
  `python3 util/style.py`; `git diff --check` passed.
- Learnings for future iterations: Response consumption should validate and
  snapshot data at one bounded boundary, while service ordering—not recursive
  event delivery—provides the later-cycle graph continuation guarantee.
---

## 2026-07-29 07:36 - US-011
- Story: Track generations and validate CommitToken snapshots
- What was implemented: Added always-on, side-effect-free CommitToken
  validation for lookup identity, line, epoch, and both SLC/SF hit, set, way,
  and generation snapshots. Added explicit epoch invalidation, preserved token
  validity across replacement-only touches, and stopped no-op sharer removal
  from advancing directory generation.
- Files changed: `src/mem/cache/CHI/HnfSLCSFBackend.hh`,
  `src/mem/cache/CHI/HnfSLCSFBackend.cc`, `src/mem/cache/CHI/HnfSLCSF.hh`,
  `src/mem/cache/CHI/HnfSLCSF.cc`, `src/mem/cache/CHI/HnfSLCSF.test.cc`,
  `prd.json`, and `progress.txt`.
- Checks run and their results: Built `build/RISCV/gem5.opt` and all four CHI
  GTest binaries successfully; all 78 tests passed (15 POCQ, 2 SEQ POCQ,
  47 SLCSF, and 14 controller), retaining all 36 baseline passes; changed C++
  files passed `python3 util/style.py`; `git diff --check` passed.
- Learnings for future iterations: Miss snapshots must validate their recorded
  candidate way directly so an unrelated LRU touch cannot stale the token,
  while generation changes still detect tag replacement, hit/miss transitions,
  committed mutations, and same-way ABA reuse.
---

## 2026-07-29 07:47 - US-012
- Story: Implement the staged mutation service
- What was implemented: Replaced placeholder non-lookup completions with an
  explicit U0 decode/validation, U1 atomic resource preparation, U2 sole array
  mutation, and U3 invariant-check/response-latch pipeline. Added typed
  dispatch for CommitRead, clean memory fill, maintenance, remove-sharer, and
  write-line semantics, with durable Error completion for malformed or
  deferred operation kinds and backpressure-safe single-write behavior.
- Files changed: `src/mem/cache/CHI/HnfSLCSFBackend.hh`,
  `src/mem/cache/CHI/HnfSLCSF.hh`, `src/mem/cache/CHI/HnfSLCSF.cc`,
  `src/mem/cache/CHI/HnfSLCSF.test.cc`, `prd.json`, and `progress.txt`.
- Checks run and their results: Built `build/RISCV/gem5.opt` and all four CHI
  GTest binaries successfully; all 86 tests passed (15 POCQ, 2 SEQ POCQ,
  55 SLCSF, and 14 controller), retaining all 36 baseline passes; changed C++
  files passed `python3 util/style.py`; `git diff --check` passed.
- Learnings for future iterations: Keeping stage ownership and terminal
  reservation in one in-flight object lets resource stalls remain response-free
  while guaranteeing a committed write cannot be repeated or blocked from its
  terminal response.
---

## 2026-07-29 07:57 - US-013
- Story: Emit side-effect-free Replay for stale mutation tokens
- What was implemented: Added owned CommitToken snapshots to Fill, Update, and
  Evict requests and an always-on U0 validation boundary. Stale snapshots now
  skip resource preparation and storage mutation, then produce a registered
  StaleCommitToken Replay with redoLookup and an overflow-checked absolute
  retry deadline derived from replay_penalty. Fresh-token mutations continue
  through U2 and complete normally.
- Files changed: `src/mem/cache/CHI/HnfSLCSFRequest.hh`,
  `src/mem/cache/CHI/HnfSLCSFResponse.hh`, `src/mem/cache/CHI/HnfSLCSF.hh`,
  `src/mem/cache/CHI/HnfSLCSF.cc`, `src/mem/cache/CHI/HnfSLCSF.test.cc`,
  `prd.json`, and `progress.txt`.
- Checks run and their results: Built `build/RISCV/gem5.opt` and all four CHI
  GTest binaries successfully; all 93 tests passed (15 POCQ, 2 SEQ POCQ,
  62 SLCSF, and 14 controller), retaining all 36 baseline passes; changed C++
  files passed `python3 util/style.py`; `git diff --check` passed.
- Learnings for future iterations: A mutation must own the exact lookup token
  it intends to consume, and stale detection belongs before both transient
  reservation and the sole persistent write stage.
---

## 2026-07-29 08:04 - US-012
- Story: Implement the staged mutation service
- What was implemented: Completed the post-commit acceptance recovery by
  making RemoveSharer participate in SF-set reservation, retaining adopted or
  acquired reservations through U2 and U3, and releasing them exactly once at
  terminal response conversion. Added side-effect-free dirty-victim preflight
  with stale writeback no-op bypass, plus resource-leak and deferred-Evict
  Error coverage.
- Files changed: `src/mem/cache/CHI/HnfSLCSFBackend.hh`,
  `src/mem/cache/CHI/HnfSLCSFBackend.cc`,
  `src/mem/cache/CHI/HnfSLCSF.cc`,
  `src/mem/cache/CHI/HnfSLCSF.test.cc`, `prd.json`, and `progress.txt`.
- Checks run and their results: Built `build/RISCV/gem5.opt` and all four CHI
  GTest binaries successfully; all 93 tests passed (15 POCQ, 2 SEQ POCQ,
  62 SLCSF, and 14 controller), retaining all 36 baseline passes; changed C++
  files passed `python3 util/style.py`; `git diff --check` passed.
- Learnings for future iterations: A mutation's transient reservation remains
  part of its in-flight ownership through invariant checking and terminal
  latching; resource preflight must reject unsupported displacement before any
  partial reservation while preserving backend-defined no-op behavior.
---

## 2026-07-29 08:24 - US-013
- Story: Emit side-effect-free Replay for stale mutation tokens
- What was implemented: Completed the acceptance recovery with independently
  retained source lookup IDs, U0 and immediate-pre-U2 token validation, and
  token-directed SLC/SF preflight and allocation. Exact-target SF reservations
  now upgrade atomically when necessary, and every terminal path releases
  adopted resources exactly once without allowing a stale persistent write.
- Files changed: `src/mem/cache/CHI/HnfSLCSFRequest.hh`,
  `src/mem/cache/CHI/HnfSLCSFBackend.hh`,
  `src/mem/cache/CHI/HnfSLCSFBackend.cc`,
  `src/mem/cache/CHI/HnfSLCSF.cc`,
  `src/mem/cache/CHI/HnfSLCSF.test.cc`, `prd.json`, and `progress.txt`.
- Checks run and their results: Built `build/RISCV/gem5.opt` and all four CHI
  GTest binaries from the newest source timestamps; all 99 tests passed
  (15 POCQ, 2 SEQ POCQ, 68 SLCSF, and 14 controller), retaining all 36
  baseline passes; changed C++ files passed `python3 util/style.py`;
  `git diff --check` passed.
- Learnings for future iterations: Never infer an expected identity from the
  untrusted token being checked. Exact-target resource preflight must ignore
  unrelated free ways, upgrade an adopted reservation atomically, and retain
  cleanup ownership on early terminal paths as well as successful writes.
---

## 2026-07-29 08:38 - US-014
- Story: Apply base per-operation service latency
- What was implemented: Added issue-time latency mapping for Lookup, Fill,
  Update, and Evict requests; normal mutations retain their staged semantics
  with deadline-driven short-latency catch-up that stops at resource stalls.
  L0 decisions are latched at issue, L0 and U0 Replay complete after one child
  cycle, later Replay never shortens the original service deadline, and replay
  penalties convert through the HomeNode clock period into absolute Ticks.
- Files changed: `src/mem/cache/CHI/HnfSLCSF.hh`,
  `src/mem/cache/CHI/HnfSLCSF.cc`,
  `src/mem/cache/CHI/HnfSLCSF.test.cc`,
  `src/mem/cache/CHI/HomeNodeFull.cc`, `prd.json`, and `progress.txt`.
- Checks run and their results: Built `build/RISCV/gem5.opt` and all four CHI
  GTest binaries successfully; all 107 tests passed (15 POCQ, 2 SEQ POCQ,
  76 SLCSF, and 14 controller), retaining all 36 baseline passes; changed C++
  files passed `python3 util/style.py`; `git diff --check` passed.
- Learnings for future iterations: A configured service latency is an earliest
  issue-to-completion deadline, so resource stalls may extend it without
  collapsing registered stage boundaries; retry deadlines require an explicit
  child-cycle-to-Tick conversion at the semantic completion boundary.
---

## 2026-07-29 08:55 - US-013
- Story: Emit side-effect-free Replay for stale mutation tokens
- What was implemented: Corrected the post-commit acceptance regression so a
  fresh mutation first acquires and owns its SF reservation in U1, then an
  intervening write stales its token before U2. Kept the separate dirty-target
  U1-stall regression proving stale Replay before any reservation is acquired.
- Files changed: `src/mem/cache/CHI/HnfSLCSF.test.cc`, `prd.json`, and
  `progress.txt`.
- Checks run and their results: Rebuilt the SLCSF GTest after the newest test
  timestamp; built `build/RISCV/gem5.opt` and all four CHI GTest binaries; all
  107 tests passed (15 POCQ, 2 SEQ POCQ, 76 SLCSF, and 14 controller); the
  changed C++ test passed `python3 util/style.py`; `git diff --check` passed.
- Learnings for future iterations: Stale-token tests must distinguish a U1
  liveness escape before resource ownership from U2 revalidation after
  resource ownership, because only the latter exercises terminal cleanup.
---

## 2026-07-29 09:21 - US-014
- Story: Apply base per-operation service latency
- What was implemented: Completed the acceptance recovery by preventing the
  sole U2 write and U3 cleanup from advancing before the configured deadline.
  Added bounded deadline catch-up, stall-aware resumption, cleanup-time Replay
  deadline construction, and explicit coverage for mapped and one/two-cycle
  Fill/Update, successful CHI Evict control flow via RemoveSharer, deferred
  typed Evict request mapping, L0/U0 Replay,
  conflict stability, stale-token timing, and short-latency resource stalls.
- Files changed: `src/mem/cache/CHI/HnfSLCSF.hh`,
  `src/mem/cache/CHI/HnfSLCSF.cc`,
  `src/mem/cache/CHI/HnfSLCSF.test.cc`, `prd.json`, and `progress.txt`.
- Checks run and their results: Built `build/RISCV/gem5.opt` and all four CHI
  GTest binaries successfully; all 111 tests passed (15 POCQ, 2 SEQ POCQ,
  80 SLCSF, and 14 controller), retaining all baseline passes; changed C++
  files passed `python3 util/style.py`; `git diff --check` passed.
- Learnings for future iterations: A latency deadline must constrain the
  architectural write boundary, not only terminal response visibility. Replay
  penalties should start when terminal cleanup makes the Replay durable.
---

## 2026-07-29 09:37 - US-015
- Story: Make read POCQ updates asynchronous
- What was implemented: Routed SLC-hit, snoop-data, and memory-fill read paths
  through explicit SLCSF CommitRead issue, wait, response-latch, and replay
  phases. CompData now waits for terminal Done; NoCredit retains one owned
  request; Replay clears attempt-derived state and waits for its absolute Tick
  before a fresh lookup and token; ReadNoSnp continues to bypass SLCSF update.
- Files changed: `src/mem/cache/CHI/HnfPOCQStateGraph.cc`,
  `src/mem/cache/CHI/HnfPOCQStateGraph.test.cc`,
  `src/mem/cache/CHI/HnfCoherencyController.hh`,
  `src/mem/cache/CHI/HnfCoherencyController.cc`,
  `src/mem/cache/CHI/HnfCoherencyController.test.cc`,
  `src/mem/cache/CHI/HomeLinkLayer.cc`, `prd.json`, and `progress.txt`.
- Checks run and their results: Built `build/RISCV/gem5.opt` and all four CHI
  GTest binaries successfully; all 115 tests passed (15 POCQ, 2 SEQ POCQ,
  80 SLCSF, and 18 controller), retaining all baseline passes; changed C++
  files passed `python3 util/style.py`; `git diff --check` passed.
- Learnings for future iterations: A controller should retain a mutation's
  owned request and identity through admission retry, hand transient resource
  ownership to the staged service, and restart from lookup only after a
  terminal Replay deadline rather than resending a stale mutation.
---

## 2026-07-29 10:02 - US-015 acceptance recovery
- Story: Make read POCQ updates asynchronous
- What was implemented: Split the actual POCQ graph into explicit
  `SlcUpdateIssue` and `SlcUpdateWait` states joined only by an accepted-request
  event. Routed Lookup and Update Replay through real graph edges to Sleep,
  made replay sleepers immune to ordinary same-address retire wakeups, gated
  deadline retries on active address hazards, and consumed visible responses
  before retry selection so a penalty-one Replay restarts on its exact eligible
  CC edge. Added exact U1 stale-token deadline/latch evidence for US-014 and
  focused controller regressions for NoCredit topology, Lookup Replay, update
  Replay, same-address retirement, and fresh request identity.
- Files changed: `src/mem/cache/CHI/HnfCcTypes.hh`,
  `src/mem/cache/CHI/HnfPOCQStateGraph.hh`,
  `src/mem/cache/CHI/HnfPOCQStateGraph.cc`,
  `src/mem/cache/CHI/HnfPOCQStateGraph.test.cc`,
  `src/mem/cache/CHI/HnfCoherencyController.hh`,
  `src/mem/cache/CHI/HnfCoherencyController.cc`,
  `src/mem/cache/CHI/HnfCoherencyController.test.cc`,
  `src/mem/cache/CHI/HnfSLCSF.test.cc`, `prd.json`, and `progress.txt`.
- Checks run and their results: Rebuilt `build/RISCV/gem5.opt` and all four CHI
  GTest binaries from the newest source timestamps; all 120 tests passed
  (17 POCQ, 2 SEQ POCQ, 80 SLCSF, and 21 controller); all changed C++ files
  passed `python3 util/style.py --modifications`; `git diff --check` passed.
- Learnings for future iterations: Side-phase bookkeeping is not a substitute
  for visible graph topology. A Replay deadline must gate every wake path, and
  response latching must precede retry selection when visibility can coincide
  with the first eligible edge.
---

## 2026-07-29 10:21 - US-016
- Story: Make maintenance POCQ updates asynchronous
- What was implemented: Routed maintenance lookup and snoop completion through
  explicit SLCSF update issue and wait states. Maintenance now sends an owned
  CompleteMaintenance request, validates its typed terminal response, and
  queues Comp only on a later controller cycle after Done. NoCredit retains
  one request and identity, while stale-token Replay releases reservations,
  waits for its absolute deadline, and restarts from a fresh lookup.
- Files changed: `src/mem/cache/CHI/HnfPOCQStateGraph.cc`,
  `src/mem/cache/CHI/HnfPOCQStateGraph.test.cc`,
  `src/mem/cache/CHI/HnfCoherencyController.cc`,
  `src/mem/cache/CHI/HnfCoherencyController.test.cc`, `prd.json`, and
  `progress.txt`.
- Checks run and their results: Built `build/RISCV/gem5.opt` and all four CHI
  GTest binaries successfully; all 124 tests passed (18 POCQ, 2 SEQ POCQ,
  80 SLCSF, and 24 controller); changed C++ files passed
  `python3 util/style.py --modifications`; `git diff --check` passed.
- Learnings for future iterations: A shared asynchronous update phase can
  serve multiple operation classes safely when request construction, response
  validation, and terminal graph edges retain concrete operation identity.
---

## 2026-07-29 10:31 - US-017
- Story: Make write completion and LinkLayer retire asynchronous
- What was implemented: Replaced direct writeLine mutation in the RXDAT path
  with an explicit lookup/token boundary and typed WriteLine update issue,
  wait, response-latch, and terminal graph states. Added a CC-owned deferred
  retire queue drained by HomeLinkLayer after periodic service, with work
  detection, Replay-safe write-data ownership, and exactly-once retirement.
- Files changed: `src/mem/cache/CHI/HnfPOCQStateGraph.cc`,
  `src/mem/cache/CHI/HnfPOCQStateGraph.test.cc`,
  `src/mem/cache/CHI/HnfCoherencyController.hh`,
  `src/mem/cache/CHI/HnfCoherencyController.cc`,
  `src/mem/cache/CHI/HnfCoherencyController.test.cc`,
  `src/mem/cache/CHI/HomeLinkLayer.cc`, `prd.json`, and `progress.txt`.
- Checks run and their results: Built `build/RISCV/gem5.opt` and all four CHI
  GTest binaries successfully; all 126 tests passed (18 POCQ, 2 SEQ POCQ,
  80 SLCSF, and 26 controller), retaining all baseline passes; changed C++
  files passed `python3 util/style.py --modifications`; `git diff --check`
  passed.
- Learnings for future iterations: A callback that supplies the final payload
  should only start an owned delayed operation; retirement produced later by
  controller service needs an explicit queue so scheduling and token release
  cannot silently discard or duplicate it.
---

## 2026-07-29 10:39 - US-018
- Story: Make eviction and remove-sharer asynchronous
- What was implemented: Routed Evict through a registered lookup followed by
  explicit SLCSF update issue and wait states. RemoveSharer now uses an owned
  typed request and exact terminal-response validation; Comp and retirement
  occur only after Done. NoCredit retains one request identity, while stale
  Replay preserves directory state and restarts from a fresh lookup after its
  absolute deadline.
- Files changed: `src/mem/cache/CHI/HnfPOCQStateGraph.cc`,
  `src/mem/cache/CHI/HnfPOCQStateGraph.test.cc`,
  `src/mem/cache/CHI/HnfCoherencyController.hh`,
  `src/mem/cache/CHI/HnfCoherencyController.cc`,
  `src/mem/cache/CHI/HnfCoherencyController.test.cc`, `prd.json`, and
  `progress.txt`.
- Checks run and their results: Built `build/RISCV/gem5.opt` and all four CHI
  GTest binaries successfully; all 129 tests passed (18 POCQ, 2 SEQ POCQ,
  80 SLCSF, and 29 controller), retaining all baseline passes; changed C++
  files passed `python3 util/style.py --modifications`; `git diff --check`
  passed.
- Learnings for future iterations: Even a directory-only protocol operation
  needs a fresh versioned lookup before delayed mutation; sharing the update
  admission and response machinery then gives NoCredit and Replay the same
  single-owner guarantees as read, maintenance, and write paths.
---
